# How to upgrade

Upgrade the charm with the `juju refresh` command:

```shell
juju refresh <pollen-operator> --channel=<channel>
```

This command pulls and applies the most recent revision of the charm from the specified channel.
